x = set(map(int, input("List 1 : ").split()))
y = set(map(int, input("List 2 : ").split())) ; n = []
for i in x:
    if i in y:
        if i not in n:
            n.append(str(i))
    
if len(n) ==0: print("tidak ada duplikat")

else :
    print(f"terdapat{len(n)} buah duplikat yaitu {','.join(n)}")

